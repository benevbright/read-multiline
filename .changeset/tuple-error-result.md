---
"@toiroakr/read-multiline": minor
---

Return `[value, error]` tuple instead of throwing errors. Remove `onCancel` option. Replace `CancelError`/`EOFError` classes with plain `{ kind, message }` objects.
