---
"@toiroakr/read-multiline": minor
---

Add `clearAfterSubmit` option (default: `true`) that clears the editor input from the terminal after submission. Set to `false` to preserve the previous behavior where input remains visible.
